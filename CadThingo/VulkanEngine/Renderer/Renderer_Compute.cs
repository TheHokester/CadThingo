// The draw-cull and light-cull compute passes that used to live here are now
// owned by DrawCullPipeline and LightCullPipeline (see Pipelines.cs). This file
// is intentionally empty; left in place so it doesn't surprise tooling that
// expects a Renderer_Compute companion.

namespace CadThingo.VulkanEngine.Renderer;